"use strict";
var remote = require('electron').remote;
var URL = require('url');
class TaskBar {
    constructor() {
        var browserWindow = remote.getCurrentWindow();
        var webView = $('#wv');
    }
}
